# AWS-Serverless-Quiz-Application
This project demonstrates a cloud-based quiz application deployed using AWS services. The frontend is hosted on EC2 with Nginx, while the backend uses API Gateway, Lambda, and DynamoDB to process and store quiz results.
